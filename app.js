const newTask = document.querySelector(".todo-button");

newTask.addEventListener("click", addNewTask);

function addNewTask(e){

    e.preventDefault();

    const newInput = document.getElementById("addTask");
    
}